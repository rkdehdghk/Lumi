// 서비스 — 옆 폴더(models)의 파일을 상위 경로로 가져다 씁니다.
// 라이브러리 안에서도 bring 을 쓸 수 있습니다.

bring "../models/user"

val admins = ["김관리", "박관리"]

func login(name):
    if incount(name, admins) > 0:
        return user.make(name, "admin")
    return user.make_guest(name)
