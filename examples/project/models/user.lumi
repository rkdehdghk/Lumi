// 자료 모형 — 사용자 하나를 만들고 다루는 곳
// (이 파일은 examples/project/models/ 안에 있습니다)

val DEFAULT_ROLE = "guest"

func make(name, role):
    return {"이름": name, "역할": role}

func make_guest(name):
    return make(name, DEFAULT_ROLE)

func show(u):
    return u["이름"] + "(" + u["역할"] + ")"
