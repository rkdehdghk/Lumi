// 자료 모형 — 사용자 하나를 만들고 다루는 곳
// (이 파일은 examples/project/models/ 안에 있습니다)

val 기본역할 = "guest"

func 만들기(이름, 역할):
    return {"이름": 이름, "역할": 역할}

func 손님만들기(이름):
    return 만들기(이름, 기본역할)

func 보기(u):
    return u["이름"] + "(" + u["역할"] + ")"
