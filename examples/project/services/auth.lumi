// 서비스 — 옆 폴더(models)의 파일을 상위 경로로 가져다 씁니다.
// 라이브러리 안에서도 bring 을 쓸 수 있습니다.

bring "../models/user"

val 관리자들 = ["김관리", "박관리"]

func 로그인(이름):
    if incount(이름, 관리자들) > 0:
        return user.만들기(이름, "admin")
    return user.손님만들기(이름)
